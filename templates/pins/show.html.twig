{% extends 'base.html.twig' %}

{% block title pin.title %}

{% block body %}
	<article>
		<h2>{{ pin.title }}</h2>
		<p>{{ pin.description }}</p>
	</article>

	<p>
		<a href="{{path('app_home')}}">Go back</a>
	</p>
{% endblock %}
